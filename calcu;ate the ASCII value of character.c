#include <stdio.h>

int main() {
    char ch = 'A';
    
    // %c displays the character, %d displays its ASCII integer value
    printf("The ASCII value of %c is %d\n", ch, ch);
    
    return 0;
}